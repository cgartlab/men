---
import BaseLayout from '../../layouts/BaseLayout.astro';
import WikiManual from '../../components/WikiManual.astro';
import SrcRef from '../../components/SrcRef.astro';
import content from '../../data/content.json';

const pageTitle = '治理 — ' + content.hero.title;
const base = '/';

const infobox = [
  { k: '团队构成', v: '<strong>6+1</strong>（men primary + 5 subagent）' },
  { k: '决策机制', v: '四类分级：架构 / 里程碑 / 日常 / 技术选型' },
  { k: '决策状态', v: '<code>已采纳</code> / <code>已回滚</code> / <code>待验证</code>' },
  { k: '变更入口', v: '<strong>PR</strong> + <code>CODEOWNERS</code> 自动评审' },
  { k: '审查者', v: '<code>@cgartlab</code>（全局默认 owner）' },
  { k: '全员红线', v: '<strong>7 条</strong>（各 agent 逐字一致）' },
  { k: '许可证', v: '<code>MIT</code>' },
];

const toc = [
  { id: 'roles', label: '角色与权限' },
  { id: 'decisions', label: '决策机制' },
  { id: 'changes', label: '变更流程' },
  { id: 'review', label: '审查' },
  { id: 'releases', label: '发布策略' },
  { id: 'security', label: '安全与合规' },
  { id: 'community', label: '社区参与' },
];

const seealso = [
  { href: base + 'docs/releases', label: '路线图', desc: '里程碑与版本发布' },
  { href: base + 'docs/architecture', label: '架构设计', desc: '拓扑、编排与验证体系' },
  { href: base + 'docs/protocols', label: '协议规范', desc: '10 步编排与机械验证' },
];
---

<BaseLayout title={pageTitle} description="men（门）Agent 团队 — 治理">
  <WikiManual
    crumb="治理"
    title="治理"
    lead="<strong>治理（governance）</strong>是 men（门）Agent 团队的协作规则总纲：以 6+1 角色权限矩阵与四类决策分级约束团队行为，以 <code>D&lt;No&gt;</code> 决策记录留存决策依据，以 PR + <code>CODEOWNERS</code> + chi 独立 Judge 构成变更与审查闭环，并以语义化版本 2.0.0 规范发布节奏。"
    infoboxTitle="治理概要"
    infobox={infobox}
    toc={toc}
    seealso={seealso}
  >
    <section id="roles" class="doc-section">
      <h2>角色与权限</h2>
      <p>治理规则定义了 6 个角色的模式、核心职责与负责维护的文件范围：</p>
      <div class="wiki-table">
        <table>
          <thead><tr><th>角色</th><th>中文名</th><th>模式</th><th>核心职责</th><th>负责维护的文件范围</th></tr></thead>
          <tbody>
            <tr>
              <td><strong>men</strong></td><td>门</td><td>primary</td>
              <td>意图分诊、任务分发、并行编排、汇总报告</td>
              <td><code>README.md、CHANGELOG.md、docs/PRD.md、docs/architecture.md、docs/governance.md、docs/guide/milestones.md、opencode.json</code></td>
            </tr>
            <tr>
              <td><strong>si</strong></td><td>思</td><td>subagent</td>
              <td>深度思考、多角度方案、产出 plan envelope、知识管理</td>
              <td><code>docs/guide/quickstart.md、docs/drafts/、docs/knowledge/</code>（沉淀与归档）</td>
            </tr>
            <tr>
              <td><strong>ji</strong></td><td>记</td><td>subagent</td>
              <td>代码实现、前端开发、内容写作、本地 gate 验证</td>
              <td><code>scripts/*.mjs、.opencode/skills/*/、.opencode/command/*.md、docs/drafts/</code></td>
            </tr>
            <tr>
              <td><strong>chi</strong></td><td>持</td><td>subagent</td>
              <td>投资分析、独立 Judge 评审、验收标准消费</td>
              <td>评审报告（如 <code>docs/m2-acceptance/chi-*.md</code>）</td>
            </tr>
            <tr>
              <td><strong>yi</strong></td><td>艺</td><td>subagent</td>
              <td>文生图提示词工程、设计决策、生图、设计 Token</td>
              <td>设计文档、docs/ 下视觉方案、图片产物</td>
            </tr>
            <tr>
              <td><strong>xun</strong></td><td>寻</td><td>subagent</td>
              <td>搜索、事实核查、RSS 扫描</td>
              <td><code>docs/research/</code>、调研与事实核查报告</td>
            </tr>
          </tbody>
        </table>
      </div>
      <ul>
        <li><strong>入口唯一</strong> — 6 个角色中仅 <strong>men 为 primary</strong>，是用户唯一对话入口；其余 5 个均为 subagent，由 men spawn。</li>
        <li><strong>定义自维护</strong> — 每个 agent 的自身定义文件（<code>.opencode/agent/&lt;名字&gt;.md</code>）由对应角色自己维护，遵守「变更流程」约束。</li>
        <li><strong>独立 Judge</strong> — chi 兼任独立 judge，以 fresh-context spawn，不沿用上游 context，保证评审客观性。</li>
      </ul>
      <SrcRef files={['docs/governance.md','一、角色与责任']} />
    </section>

    <section id="decisions" class="doc-section">
      <h2>决策机制</h2>
      <h3>决策类型</h3>
      <p>治理将决策分为四类，按影响面分配决策者与记录方式：</p>
      <div class="wiki-table">
        <table>
          <thead><tr><th>类型</th><th>示例</th><th>决策者</th><th>记录方式</th></tr></thead>
          <tbody>
            <tr><td><strong>架构决策</strong></td><td>是否新增角色、是否迁移运行时</td><td>men + 核心成员讨论</td><td><code>docs/architecture.md</code> 决策记录表</td></tr>
            <tr><td><strong>里程碑决策</strong></td><td>M5→M6 启动条件</td><td>men</td><td><code>docs/guide/milestones.md</code></td></tr>
            <tr><td><strong>日常决策</strong></td><td>某个 skill 的修改</td><td>对应 agent</td><td><code>scripts/event.mjs</code> 写入 events.jsonl</td></tr>
            <tr><td><strong>技术选型</strong></td><td>脚本语言、MCP 选择</td><td>men + ji</td><td><code>docs/research/00-m0-synthesis.md</code></td></tr>
          </tbody>
        </table>
      </div>
      <p>判断规则：</p>
      <ul>
        <li>涉及多角色影响面（新增/删除角色、改运行时、改流程）→ 架构决策级别。</li>
        <li>仅影响单个 skill / 单个脚本的局部修改 → 对应 agent 自行决策，但必须留事件审计。</li>
        <li>低置信时先向用户确认，不猜。</li>
      </ul>
      <h3>决策记录</h3>
      <p>重要决策写入 <code>docs/architecture.md</code> 的「技术决策记录」表格，包含：决策编号、决策内容、依据、状态。</p>
      <ul>
        <li>决策编号格式：<code>D&lt;No&gt;</code>（如 D1–D6，已有 6 条沉淀在 milestones.md 与 architecture.md）。</li>
        <li>状态枚举：<code>已采纳</code> / <code>已回滚</code> / <code>待验证</code>。</li>
        <li>架构决策未经 men + 用户确认前，不得进入代码或文档正文。</li>
      </ul>
      <h3>回滚机制</h3>
      <p>某次决策导致回归（REGRESSED），需要回滚时：</p>
      <ol>
        <li>git revert 回退到上一版本</li>
        <li>在 <code>docs/governance.md</code> 的「决策记录」中记录回滚原因</li>
        <li>更新 <code>CHANGELOG.md</code></li>
      </ol>
      <p>回滚触发条件包括：</p>
      <ul>
        <li>chi judge 判定 REGRESSED 且人工确认无法修复。</li>
        <li>机械验证退出码非 0，且 5 次强化后仍失败。</li>
        <li>用户明确要求放弃当前方案。</li>
      </ul>
      <SrcRef files={['docs/governance.md','二、决策流程']} />
    </section>

    <section id="changes" class="doc-section">
      <h2>变更流程</h2>
      <p>按变更对象分类，四类变更各有强制要求：</p>
      <h3>Agent 定义变更</h3>
      <p>修改 <code>.opencode/agent/*.md</code> 时必须遵守：</p>
      <ol>
        <li>先 read 再 edit</li>
        <li>保留 YAML frontmatter（description / mode / model）</li>
        <li>保留 CHARTER_CHECK 字段</li>
        <li>全员红线逐字一致</li>
        <li>模型分配以 <code>opencode.json</code> 的 agent 字段为准</li>
      </ol>
      <h3>Skill 包变更</h3>
      <p>修改 <code>.opencode/skills/*/SKILL.md</code> 时必须：</p>
      <ol>
        <li>保留 "Use when" 描述和「不要触发」节</li>
        <li>步骤工作流编号连续</li>
        <li>项目规范参考节保持一致的格式</li>
      </ol>
      <h3>脚本变更</h3>
      <p>修改 <code>scripts/*.mjs</code> 后必须：</p>
      <ol>
        <li>运行 <code>node scripts/verify.mjs .</code> 验证自身</li>
        <li>如果修改了 gate 规则，必须更新 <code>docs/architecture.md</code> 的验证体系描述</li>
        <li>如果修改了 event 结构，必须更新 PRD.md 和 milestones.md</li>
      </ol>
      <h3>配置变更</h3>
      <p>修改 <code>opencode.json</code> 后必须：</p>
      <ol>
        <li>验证 JSON 格式（<code>node -e "JSON.parse(...)"</code>）</li>
        <li>同步更新 README.md 和 architecture.md 中对应的描述</li>
      </ol>
      <SrcRef files={['docs/governance.md','三、变更管理']} />
    </section>

    <section id="review" class="doc-section">
      <h2>审查</h2>
      <h3>审查流程</h3>
      <p>从开发提交 PR 到合并的完整审查链：</p>
      <ol>
        <li><strong>提交 PR</strong> — 开发提交 PR，<code>CODEOWNERS</code> 自动请求评审</li>
        <li><strong>自审清单确认</strong> — 提交人对照自审清单逐项确认</li>
        <li><strong>机械检查</strong> — <code>verify.mjs</code> 执行机械检查</li>
        <li><strong>语义复核</strong> — chi judge 以 fresh-context 做语义复核</li>
        <li><strong>裁决</strong> — PASS 则合并；FAIL / REGRESSED 则修复后重新提交；BLOCKED 则停止并汇报卡住</li>
      </ol>
      <div class="wiki-note">
        <strong>审查裁决</strong>：<code>PASS</code> = 合并；<code>FAIL / REGRESSED</code> = 修复后重新提交；<code>BLOCKED</code> = 停止，汇报卡住。
      </div>
      <h3>审查标准</h3>
      <div class="wiki-table">
        <table>
          <thead><tr><th>标准</th><th>检查项</th></tr></thead>
          <tbody>
            <tr><td><strong>机械验证</strong></td><td>verify.mjs 5 项全 PASS，退出码 0</td></tr>
            <tr><td><strong>红线遵守</strong></td><td>7 条全员红线无一违反</td></tr>
            <tr><td><strong>一致性</strong></td><td>agent 定义中全员红线逐字一致</td></tr>
            <tr><td><strong>完整性</strong></td><td>frontmatter / CHARTER_CHECK / 项目规范参考节完整</td></tr>
            <tr><td><strong>无回归</strong></td><td>相比上一版本无 REGRESSED</td></tr>
          </tbody>
        </table>
      </div>
      <h3>合并策略</h3>
      <ul>
        <li>默认 squash merge</li>
        <li>合并后删除分支</li>
        <li>禁止 force push</li>
      </ul>
      <SrcRef files={['docs/governance.md','四、代码审查','CODEOWNERS']} />
    </section>

    <section id="releases" class="doc-section">
      <h2>发布策略</h2>
      <h3>版本策略</h3>
      <p>遵循 <a href="https://semver.org/lang/zh-CN/" target="_blank" rel="noopener">语义化版本 2.0.0</a>：</p>
      <div class="wiki-table">
        <table>
          <thead><tr><th>版本类型</th><th>触发条件</th></tr></thead>
          <tbody>
            <tr><td><strong>MAJOR</strong></td><td>不兼容变更（如角色变更、协议变更）</td></tr>
            <tr><td><strong>MINOR</strong></td><td>向后兼容的新功能（如新增 skill、新增命令）</td></tr>
            <tr><td><strong>PATCH</strong></td><td>向后兼容的修复（如 bugfix、文档修正）</td></tr>
          </tbody>
        </table>
      </div>
      <h3>发布流程</h3>
      <ol>
        <li>确认所有里程碑验收通过</li>
        <li>运行 <code>npm run release &lt;type&gt;</code> 执行 SemVer bump + CHANGELOG + git tag</li>
        <li>推送 tag 到 GitHub</li>
        <li>在 GitHub Release 页面填写发布说明</li>
        <li>更新 README.md 中的里程碑状态</li>
      </ol>
      <h3>发布检查清单</h3>
      <ul>
        <li><code>node scripts/verify.mjs .</code> 全部 PASS</li>
        <li>CHANGELOG.md 已更新</li>
        <li>README.md 里程碑状态已更新</li>
        <li>LICENSE 文件存在</li>
        <li>.github/ 模板文件完整</li>
      </ul>
      <SrcRef files={['docs/governance.md','五、发布管理']} />
    </section>

    <section id="security" class="doc-section">
      <h2>安全与合规</h2>
      <h3>安全基线</h3>
      <ul>
        <li><code>.env</code> 文件不得提交到版本控制</li>
        <li>API key / 个人信息不得出现在任何文档或代码中</li>
        <li>内网数据源（元搜索聚合）仅限 <code>局域网网段（不对外公开）</code> 内网访问</li>
        <li>依赖安装限定在 <code>.opencode/node_modules/</code>，不污染根目录</li>
        <li>安全漏洞报告见 <code>SECURITY.md</code></li>
      </ul>
      <h3>漏洞报告</h3>
      <ul>
        <li><strong>GitHub Issue</strong> — 提交一个标记为 <code>security</code> 的 Issue，描述漏洞细节</li>
        <li><strong>邮件</strong> — 发送漏洞详情至 <code>hello@cgartlab.com</code></li>
        <li><strong>响应承诺</strong> — 72 小时内响应报告</li>
      </ul>
      <div class="wiki-table">
        <table>
          <thead><tr><th>阶段</th><th>时间</th><th>动作</th></tr></thead>
          <tbody>
            <tr><td>确认</td><td>24h 内</td><td>确认收到报告，评估严重程度</td></tr>
            <tr><td>复现</td><td>48h 内</td><td>本地复现漏洞，定位根因</td></tr>
            <tr><td>修复</td><td>72h 内</td><td>提交修复 PR，回关 Issue</td></tr>
            <tr><td>披露</td><td>修复后</td><td>公开披露漏洞细节与修复方案</td></tr>
          </tbody>
        </table>
      </div>
      <h3>依赖安全</h3>
      <ul>
        <li><code>.opencode/node_modules/</code> 本地安装，不共享根目录</li>
        <li>根目录 <code>package.json</code> 仅作 npm 发布载体，不含运行时依赖</li>
        <li>升级依赖前请核查 npm audit 结果</li>
      </ul>
      <SrcRef files={['docs/governance.md','六、安全策略','SECURITY.md']} />
    </section>

    <section id="community" class="doc-section">
      <h2>社区参与</h2>
      <h3>Issue 管理</h3>
      <ul>
        <li>Bug 报告使用 <code>bug_report.md</code> 模板</li>
        <li>功能建议使用 <code>feature_request.md</code> 模板</li>
        <li>每个 issue 必须指定对应的角色（men / si / ji / chi / yi / xun）</li>
      </ul>
      <h3>PR 管理</h3>
      <ul>
        <li>遵循 <code>CONTRIBUTING.md</code>（分支命名 dev-/write-/fix-/chore-，Conventional Commits）</li>
        <li>必须通过 <code>CODEOWNERS</code> 自动评审流程</li>
        <li>必须通过 <code>verify.mjs</code> 机械检查</li>
      </ul>
      <h3>行为准则</h3>
      <ul>
        <li>见 <code>CODE_OF_CONDUCT.md</code></li>
        <li>尊重、包容、协作</li>
        <li>禁止骚扰、歧视、人身攻击</li>
      </ul>
      <h3>文档维护</h3>
      <div class="wiki-table">
        <table>
          <thead><tr><th>文档</th><th>更新时机</th><th>负责人</th></tr></thead>
          <tbody>
            <tr><td><code>README.md</code></td><td>每次发布</td><td>men</td></tr>
            <tr><td><code>CHANGELOG.md</code></td><td>每次提交后合并前</td><td>men</td></tr>
            <tr><td><code>docs/PRD.md</code></td><td>里程碑变更</td><td>men</td></tr>
            <tr><td><code>docs/architecture.md</code></td><td>架构决策变更</td><td>men</td></tr>
            <tr><td><code>docs/guide/quickstart.md</code></td><td>安装/命令变更</td><td>si</td></tr>
            <tr><td><code>docs/guide/milestones.md</code></td><td>里程碑验收</td><td>men</td></tr>
            <tr><td><code>docs/governance.md</code></td><td>治理规则变更</td><td>men</td></tr>
            <tr><td><code>SECURITY.md</code></td><td>安全策略变更</td><td>men</td></tr>
            <tr><td><code>CONTRIBUTING.md</code></td><td>贡献流程变更</td><td>men</td></tr>
          </tbody>
        </table>
      </div>
      <p>更新原则：文档与代码同步提交，禁止「代码先合、文档后补」；所有文档遵循第 7 条红线（粗体关键信息、列表优先、单段 ≤6 行）；治理规则有冲突时，以文档版本号最新者为准，并在 <code>CHANGELOG.md</code> 记录变更。</p>
      <SrcRef files={['docs/governance.md','七、社区参与','八、文档维护','CONTRIBUTING.md','CODE_OF_CONDUCT.md']} />
    </section>
  </WikiManual>
</BaseLayout>
