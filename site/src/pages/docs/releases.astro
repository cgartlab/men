---
import BaseLayout from '../../layouts/BaseLayout.astro';
import WikiManual from '../../components/WikiManual.astro';
import SrcRef from '../../components/SrcRef.astro';
import content from '../../data/content.json';
import { SITE } from '../../data/site';

const pageTitle = '路线图 — ' + content.hero.title;
const base = '/';

const infobox = [
  { k: '当前版本', v: `<code>${SITE.version}</code>（2026-08-29 发布）` },
  { k: '里程碑', v: '<strong>M0–M7</strong> 全部完成' },
  { k: '许可证', v: '<code>MIT</code>' },
  { k: '版本规范', v: 'SemVer 2.0.0（<code>X.Y.Z</code>）' },
  { k: '版本载体', v: '<code>package.json</code> + <code>CHANGELOG.md</code> + git tag' },
  { k: '发布通道', v: 'GitHub Releases' },
  { k: 'CI 自动化', v: 'GitHub Actions（<code>ci.yml</code> 含 release dry-run）' },
];

const toc = [
  { id: 'version-history', label: '版本历史' },
  { id: 'current-release', label: '当前版本亮点' },
  { id: 'milestones', label: '里程碑进度' },
  { id: 'release-process', label: '发布流程' },
  { id: 'future', label: '未来规划' },
];

const seealso = [
  { href: base + 'docs/governance', label: '治理', desc: '角色权限与发布策略' },
  { href: base + 'docs/architecture', label: '架构设计', desc: '拓扑与验证体系全景' },
  { href: base + 'docs/overview', label: '概览', desc: '返回文档入口' },
];
---

<BaseLayout title={pageTitle} description="men（门）Agent 团队 — 路线图与版本发布">
  <WikiManual
    crumb="路线图"
    title="路线图"
    lead="<strong>men（门）</strong>是一个持续演进的开源 Agent 团队系统：本文档记录其版本发布历史、当前版本内容、M0–M7 里程碑进度与基于 SemVer 2.0.0 的发布流程，是理解项目演进脉络的编年索引。"
    infoboxTitle="版本概要"
    infobox={infobox}
    toc={toc}
    seealso={seealso}
  >
    <section id="version-history" class="doc-section">
      <h2>版本历史</h2>
      <p>
        项目的全部已发布版本记录于 <code>CHANGELOG.md</code>，格式遵循
        <a href="https://keepachangelog.com/zh-CN/1.1.0/" target="_blank" rel="noopener">Keep a Changelog</a>，
        版本号遵循语义化版本 2.0.0。每个版本按六大类组织：新增 / 变化 / 修复 / 移除 / 弃用 / 安全。
      </p>
      <div class="wiki-table">
        <table>
          <thead><tr><th>版本</th><th>日期</th><th>要点</th></tr></thead>
          <tbody>
            <tr>
              <td><strong>v0.3.3</strong></td>
              <td>2026-08-29</td>
              <td>发布主题「品牌升级 + 架构重构」：全量 rebrand 为「Men Agent 团队」；删除手写调度器 <code>src/</code>（−1433 行）转向 OpenCode 原生插件；新增 men-verify / men-learn 自动化插件；修复死脚本与文档步数（9→10）等诚实性问题。</td>
            </tr>
            <tr>
              <td><strong>v0.3.2</strong></td>
              <td>2026-08-28</td>
              <td>发布主题「侧边栏全局化」：新增 <code>install.mjs --global</code> 把 <code>@cgartlab/men</code> 注册进全局 <code>tui.json</code>，任意目录打开 OpenCode 即显示 Men 侧边栏（不触碰 CC Switch 管理的 opencode.json）。</td>
            </tr>
            <tr>
              <td><strong>v0.3.1</strong></td>
              <td>2026-08-27</td>
              <td>发布主题「侧边栏修复」：新增 <code>.opencode/tui.json</code> 声明 TUI 插件入口（TUI 插件无目录自动发现，必须显式列出），补 <code>@opentui/*</code> 依赖。</td>
            </tr>
            <tr>
              <td><strong>v0.3.0</strong></td>
              <td>2026-08-27</td>
              <td>发布主题「一行命令安装」：发布 npm 包 <code>@cgartlab/men</code>，任意目录 <code>npx @cgartlab/men</code> 一步安装；安装指令准确性修复（URL 硬编码、install.astro 修复、<code>.opencode/package.json</code> 纳入版本控制、4 个 MCP 补齐 transport）；文档一致性同步；明确 Men=OpenCode 插件、模型密钥由 CC Switch 本地托管。</td>
            </tr>
            <tr>
              <td><strong>v0.2.1</strong></td>
              <td>2026-08-27</td>
              <td>发布主题「角色定位重构」：men 确立为唯一编排核心（子角色不直接互编）、si 转向思考者与知识管理者（写作移交 ji）、yi 核心转文生图提示词工程；技能计数 14→13 修正；hyperplan 术语同步。</td>
            </tr>
            <tr>
              <td><strong>v0.2.0</strong></td>
              <td>2026-08-21</td>
              <td>发布主题「从代码仓库到可协作团队」：M5 文档完善、M6 GitHub 开源基础设施（含 MIT 许可证与 CI）、M7 自主学习回路；修复文档与元数据不一致；许可证由 Apache-2.0 改为 MIT。</td>
            </tr>
            <tr>
              <td><strong>v0.1.0</strong></td>
              <td>2026-08-20</td>
              <td>发布主题「首发：6+1 Agent 团队系统」：M0 调研、M1 骨架、M2 单兵验收、M3 编排、M4 机械验证，以及全平台安装与版本发布脚本。</td>
            </tr>
            <tr>
              <td><code>[Unreleased]</code></td>
              <td>—</td>
              <td>下一版本的新增内容占位，当前为空，尚无已确定的版本号与发布日期。</td>
            </tr>
          </tbody>
        </table>
      </div>
      <div class="wiki-note">
        截至 2026-08-29，共发布 <strong>7 个正式版本</strong>（v0.1.0、v0.2.0、v0.2.1、v0.3.0、v0.3.1、v0.3.2、v0.3.3）。
        当前版本为 <code>{SITE.version}</code>（site/package.json，经 <code>SITE</code> 单一来源注入）。
      </div>
      <SrcRef files={['CHANGELOG.md','site/src/data/content.json']} />
    </section>

    <section id="current-release" class="doc-section">
      <h2>当前版本亮点</h2>
      <p>
        当前版本 <code>v0.3.3</code> 于 2026-08-29 发布，主题为「品牌升级 + 架构重构」：全量 rebrand 为「Men Agent 团队」，并删除手写调度器、转向 OpenCode 原生插件架构。
      </p>
      <h3>v0.3.3「品牌升级 + 架构重构」</h3>
      <ul>
        <li><strong>品牌 rebrand</strong>：agent/command 定义全量同步为「Men Agent 团队」（8 处）</li>
        <li><strong>架构重构</strong>：删除手写调度器 <code>src/</code>（−1433 行），转向 OpenCode 原生 agent/command/skill/plugin 组合</li>
        <li><strong>men-verify / men-learn 自动化插件</strong>：写产物后自动跑机械验证、任务完成后自动提取经验</li>
        <li><strong>诚实性修复</strong>：README 协议步数 9→10、死脚本改相对路径、事件 type 对齐 14 种 KINDS、插件日志改文件写入消除 UI 污染</li>
      </ul>
      <h3>v0.3.2「侧边栏全局化」</h3>
      <ul>
        <li><strong>install.mjs --global</strong>：把 <code>@cgartlab/men</code> 注册进 OpenCode 全局 <code>tui.json</code>，任意目录打开 OpenCode 即显示 Men 侧边栏</li>
        <li><strong>仅改 tui.json</strong>：不触碰 CC Switch 管理的 opencode.json，模型与密钥托管不受影响</li>
        <li><strong>补 exports 与 @opentui 依赖</strong>：根 package.json 补 <code>exports["./server"]</code>/<code>exports["./tui"]</code>，OpenCode 启动自动安装 npm 包后加载侧边栏</li>
      </ul>
      <p class="wiki-note">
        以下为 v0.3.0（历史）版本的发布亮点回顾。
      </p>
      <h3>v0.3.0「一行命令安装」</h3>
      <ul>
        <li><strong>npm 包发布与 npx 一行安装</strong>：任意目录执行 <code>npx @cgartlab/men</code>，自动完成 scaffold + 依赖 + <code>.env</code> + 端到端验证</li>
        <li><strong>安装指令准确性修复</strong>：一键脚本 URL 硬编码、install.astro 标签修复、<code>.opencode/package.json</code> 纳入版本控制、4 个 MCP 服务器补齐 transport 全部可加载</li>
        <li><strong>文档与站点全面同步</strong>：事件 14 种、技能 15 个、MCP×7、版本引用 v0.3.0</li>
        <li><strong>定位澄清</strong>：Men 是 OpenCode 插件，模型与密钥由 CC Switch 本地托管，安装无需密钥</li>
      </ul>
      <p class="wiki-note">
        以下内容为 <strong>v0.2.0（历史）</strong>版本的发布亮点回顾。
      </p>
      <h3>M5 文档完善</h3>
      <p>
        补齐核心文档：产品需求文档（PRD）、架构说明（含 D1–D17 技术决策记录）、快速上手指南、
        里程碑进度、版本发布方案等，为新人提供理解项目的第一入口。
      </p>
      <h3>M6 GitHub 开源基础设施</h3>
      <ul>
        <li><strong>标准文档全套</strong>：<code>LICENSE</code>（MIT）、<code>CONTRIBUTING.md</code>、<code>SECURITY.md</code>（承诺 72 小时响应）、<code>CODE_OF_CONDUCT.md</code></li>
        <li><strong>PR / Issue 模板</strong>、<code>CODEOWNERS</code> 自动分派、<code>FUNDING.yml</code>、Dependabot 依赖自动更新</li>
        <li><strong>CI 工作流</strong>：每次 push / PR 自动执行脚本语法检查 + verify + release dry-run + installer smoke</li>
        <li><strong>团队治理文档</strong>：<code>docs/governance.md</code> 定义角色权限、决策流程、变更管理与发布策略</li>
      </ul>
      <h3>M7 自主学习回路</h3>
      <ul>
        <li><strong>学习循环</strong>：<code>learn.mjs</code> 在每次 /ultrawork 后从 events.jsonl 提取经验，写入 <code>knowledge/errors/</code> 与 <code>knowledge/patterns/</code></li>
        <li><strong>评估指标</strong>：<code>eval-metrics.mjs</code> 在 /verify 后计算 8 项 KPI（通过率、回归率等）</li>
        <li><strong>知识库</strong>：3 条决策记录（M0/M6/M7）、3 条初始协作模式、1 条自动提取错误</li>
        <li><strong>MCP 工具扩充</strong>：由 3 个增加到 7 个（新增 fetch / github-mcp-server / memory / sequential-thinking）</li>
      </ul>
      <h3>发布时的验证结果</h3>
      <div class="wiki-table">
        <table>
          <thead><tr><th>验证项</th><th>结果</th></tr></thead>
          <tbody>
            <tr><td>全部 11 个脚本 <code>node --check</code></td><td>全部通过</td></tr>
            <tr><td><code>ci.yml</code> 最近 5 次 CI run</td><td>全部 success</td></tr>
            <tr><td>chi judge 核心文档验收</td><td>7/7 全部 PASS</td></tr>
          </tbody>
        </table>
      </div>
      <SrcRef files={['CHANGELOG.md','docs/guide/milestones.md']} />
    </section>

    <section id="milestones" class="doc-section">
      <h2>里程碑进度</h2>
      <p>
        项目以里程碑（M0–M7）推进，全部已收官。以下为官方进度表述（来自
        <code>content.json</code> 的 milestones 数组）：
      </p>
      <div class="wiki-table">
        <table>
          <thead><tr><th>里程碑</th><th>名称</th><th>状态</th><th>摘要</th></tr></thead>
          <tbody>
            {content.milestones.map((ms) => (
              <tr>
                <td><code>{ms.m}</code></td>
                <td><strong>{ms.name}</strong></td>
                <td>{ms.status}</td>
                <td>{ms.detail}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <div class="wiki-note">
        时间线：M0–M4 于 2026-08-15 完成（调研 / 骨架 / 单兵 / 编排 / 机械验证），
        M5–M7 于 2026-08-21 完成（文档 / GitHub 基础设施 / 自主学习回路），
        与 v0.1.0、v0.2.0、v0.2.1、v0.3.0、v0.3.1、v0.3.2、v0.3.3 各版本的发布时间对应。
      </div>
      <p class="src-ref">来源：<code>site/src/data/content.json</code>（milestones）· <code>docs/guide/milestones.md</code></p>
    </section>

    <section id="release-process" class="doc-section">
      <h2>发布流程</h2>
      <p>版本发布遵循 <code>docs/guide/release.md</code>，版本载体为根目录 <code>package.json</code> + <code>CHANGELOG.md</code> + git tag。</p>

      <h3>版本号约定（SemVer 2.0.0）</h3>
      <div class="wiki-table">
        <table>
          <thead><tr><th>版本段</th><th>含义</th><th>示例</th></tr></thead>
          <tbody>
            <tr><td><strong>major</strong>（主版本）</td><td>不兼容的架构 / 行为变更</td><td><code>0.1.0 → 1.0.0</code></td></tr>
            <tr><td><strong>minor</strong>（次版本）</td><td>向后兼容的功能新增</td><td><code>0.1.0 → 0.2.0</code></td></tr>
            <tr><td><strong>patch</strong>（修订号）</td><td>向后兼容的缺陷修复</td><td><code>0.1.0 → 0.1.1</code></td></tr>
          </tbody>
        </table>
      </div>

      <h3>一键发布</h3>
      <div class="code-block">
        <span class="code-block__lang">bash</span>
        <pre><code>npm run release            # patch（默认）
npm run release:minor      # 0.2.1 → 0.3.0
npm run release:major      # 0.1.0 → 1.0.0
npm run release:all        # 全部 = --push + --gh-release + --npm</code></pre>
      </div>
      <p><code>scripts/release.mjs</code> 自动完成以下步骤：</p>
      <ol>
        <li>读取 <code>package.json</code> 的 version 并校验 SemVer（非法退出码 2）</li>
        <li>bump 版本号并写回 <code>package.json</code></li>
        <li>在 <code>CHANGELOG.md</code> 的 <code>[Unreleased]</code> 下插入新版本节，迁移已整理条目</li>
        <li>同步版本号到 JSON/文本文件（<code>opencode.json</code>、<code>configure.astro</code>、<code>AGENTS.md</code> 等 8 处）</li>
        <li>若已 <code>git init</code>：<code>git add</code> + <code>git commit -m "chore(release): vX.Y.Z"</code> + <code>git tag vX.Y.Z</code></li>
        <li>用 <code>scripts/release-notes.mjs</code> 提取只含当前版本的 release notes</li>
      </ol>
      <div class="wiki-note">
        <strong>推送与发布由 flag 控制</strong>：默认不推送不发布，由维护者确认后执行。
        使用 <code>--push</code> 自动推送、<code>--gh-release</code> 自动创建 GitHub Release、<code>--npm</code> 自动发布 npm、<code>--all</code> 全部执行。
        使用 <code>--push</code> 或 <code>--gh-release</code> 时，<code>update-release-page.mjs</code> 自动同步 <code>releases.astro</code>（版本行 + 高亮 + 日期 + 计数）。
      </div>

      <h3>GitHub Releases 关联</h3>
      <p>
        使用 <code>--gh-release</code> flag 时，脚本自动调用 <code>gh release create</code> 创建 GitHub Release，
        release notes 使用 <code>release-notes.mjs</code> 提取的只含当前版本内容。
        也可手动使用 <code>scripts/release-notes.mjs</code> 提取后创建：
      </p>
      <div class="code-block">
        <span class="code-block__lang">bash</span>
        <pre><code># 自动（推荐）
node scripts/release.mjs patch --push --gh-release

# 手动提取 + 创建
node scripts/release-notes.mjs --output /tmp/release-notes.md
gh release create vX.Y.Z --title "vX.Y.Z — 主题" --notes-file /tmp/release-notes.md</code></pre>
      </div>

      <h3>CI 自动验证</h3>
      <p>
        <code>.github/workflows/ci.yml</code> 的 validate job 在每次 push / PR 时执行
        <code>node scripts/release.mjs --dry-run</code>（release dry-run），
        确保发版脚本本身可通过机械验证；另有 PR 自动打标与指派。
      </p>
      <SrcRef files={['docs/guide/release.md','.github/workflows/ci.yml']} />
    </section>

    <section id="future" class="doc-section">
      <h2>未来规划</h2>
      <p>
        里程碑文档（<code>docs/guide/milestones.md</code>）在「下一步」中列出了 M8+ 的规划方向，
        尚未确定版本号与发布日期：
      </p>
      <ul>
        <li><strong>跨运行时 adapter</strong>：当前仅适配 OpenCode，规划适配 Codex / OpenClaw 等运行时</li>
        <li><strong>插件化封装</strong>：将 agent / skill / command 体系封装为可复用插件</li>
        <li><strong>Agent 团队扩展</strong>：扩展更多角色与协作模式</li>
      </ul>
      <div class="wiki-note">
        以上为规划方向，具体排期与版本对应以后续发布的 <code>CHANGELOG.md</code> 与 GitHub Releases 为准。
      </div>
      <p class="src-ref">来源：<code>docs/guide/milestones.md</code>（下一步）· <code>docs/architecture.md</code>（决策 D2）</p>
    </section>
  </WikiManual>
</BaseLayout>
