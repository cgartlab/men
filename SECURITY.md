# 安全策略

我们高度重视项目安全。如果你发现了安全漏洞，请通过以下方式报告。

---

## 报告漏洞

- **GitHub Issue** — 提交一个标记为 `security` 的 Issue，描述漏洞细节
- **邮件** — 发送漏洞详情至 `hello@cgartlab.com`

我们会在 **72 小时内** 响应你的报告。

---

## 范围

本安全策略适用于仓库内所有代码，包括但不限于：

| 目录/文件 | 说明 |
|-----------|------|
| `scripts/` | 机械验证、门禁、事件审计脚本 |
| `.opencode/` | Agent 定义、技能包、自定义命令 |
| `config/` | MCP 等配置文件 |
| `install.sh` / `install.ps1` | 一键安装脚本 |

---

## 敏感信息

### API Key 与凭证

- `.env` 文件包含各类 API key，**必须**加入 `.gitignore`
- **严禁**将 `.env`、`.env.local` 或任何含密钥的文件提交到版本控制
- 本地开发请从 `.env.example` 复制后填写

### 内网数据源

以下数据源仅限内网（`192.168.31.x`）访问，**不可暴露到公网**：

- **SearXNG** — 本地搜索引擎
- **Miniflux** — RSS 阅读器
- **Wealth Tracker** — 个人财务数据

---

## 依赖安全

- `.opencode/node_modules/` 本地安装，**不共享根目录**
- 根目录 `package.json` 仅作 npm 发布载体，不含运行时依赖
- 升级依赖前请核查 npm audit 结果

---

## 事件响应

| 阶段 | 时间 | 动作 |
|------|------|------|
| 确认 | 24h 内 | 确认收到报告，评估严重程度 |
| 复现 | 48h 内 | 本地复现漏洞，定位根因 |
| 修复 | 72h 内 | 提交修复 PR，回关 Issue |
| 披露 | 修复后 | 公开披露漏洞细节与修复方案 |

严重漏洞（密钥泄露、远程执行等）将优先处理并发布安全公告。
