---
import BaseLayout from '../../layouts/BaseLayout.astro';
import WikiManual from '../../components/WikiManual.astro';
import SrcRef from '../../components/SrcRef.astro';
import content from '../../data/content.json';
import { SITE } from '../../data/site';

const pageTitle = '快速上手 — ' + content.hero.title;
const base = '/';

const infobox = [
  { k: '前置条件', v: '<code>Node.js ≥ 18</code> + 已安装 OpenCode' },
  { k: '核心命令', v: '<code>/ultrawork</code> · <code>/verify</code> · <code>/hyperplan</code>' },
  { k: '编排协议', v: '<strong>9 步</strong>核心（CERTAINTY → LOOP）+ 第 10 步 LEARN（M7）' },
  { k: '验证机制', v: '<strong>双层验证</strong>：verify.mjs 五项机械检查 + chi 语义复核' },
  { k: '预计耗时', v: '约 <strong>30 分钟</strong>跑通第一个任务（模型配置约 2–3 分钟）' },
  { k: '模型配置', v: '<code>node scripts/setup.mjs</code> 交互式引导' },
  { k: '当前版本', v: `<code>${SITE.version}</code>` },
];

const toc = [
  { id: 'prerequisites', label: '前置条件' },
  { id: 'clone', label: '克隆与启动' },
  { id: 'first-task', label: '第一个任务' },
  { id: 'verify', label: '验收产物' },
  { id: 'hyperplan', label: '规划复杂项目' },
  { id: 'next', label: '下一步' },
];

const seealso = [
  { href: base + 'docs/install', label: '安装', desc: '多平台安装命令' },
  { href: base + 'docs/configure', label: '配置', desc: '模型连接与配置' },
  { href: base + 'docs/agents', label: '角色说明', desc: '6 个 agent 详解' },
  { href: base + 'docs/protocols', label: '协议规范', desc: '验证与审计' },
  { href: base + 'docs/architecture', label: '架构设计', desc: '拓扑与流程' },
];
---

<BaseLayout title={pageTitle} description="men（门）Agent 团队 — 快速上手">
  <WikiManual
    crumb="快速上手"
    title="快速上手"
    lead="本文介绍从克隆仓库到完成第一个任务的完整流程：配置运行环境、启动本地站点，并通过 <code>/ultrawork</code>、<code>/verify</code>、<code>/hyperplan</code> 三个命令驱动 men（门）Agent 团队完成一键编排、独立验收与复杂项目规划。"
    infoboxTitle="快速上手信息"
    infobox={infobox}
    toc={toc}
    seealso={seealso}
  >
    <section id="prerequisites" class="doc-section">
      <h2>前置条件</h2>
      <p>开始前需要满足以下运行环境：</p>
      <ul>
        <li><strong>Node.js ≥ 18</strong> — <code>@opencode-ai/plugin</code> 的 engines 要求，脚本依赖在此版本上运行。</li>
        <li><strong>OpenCode</strong> — 已安装并准备进入 men 项目目录；启动后自动读取 <code>opencode.json</code>，将 men 设为默认 agent。</li>
        <li><strong>模型 API Key</strong> — 通过引导脚本 <code>node scripts/setup.mjs</code> 交互式配置（约 2–3 分钟），脚本会根据订阅情况推荐模型组合并写入 <code>opencode.json</code>。</li>
      </ul>
      <SrcRef files={['AGENTS.md','docs/guide/quickstart.md']} />
    </section>

    <section id="clone" class="doc-section">
      <h2>克隆与启动</h2>
      <p>克隆仓库并启动本地站点：</p>
      <div class="code-block">
        <span class="code-block__lang">bash</span>
        <pre><code>git clone https://github.com/cgartlab/men.git
cd men
npx astro dev --config site/astro.config.mjs</code></pre>
      </div>
      <p>打开 <a href="http://localhost:4399/">http://localhost:4399/</a> 即可预览站点。</p>
      <p>正式环境地址为 <strong><code>https://men.cgartlab.com</code></strong>（上线后生效），本地开发与线上站点内容一致。</p>
      <p>如需将 agent / skills / commands 同步到 OpenCode 全局，可执行：</p>
      <div class="code-block">
        <span class="code-block__lang">powershell</span>
        <pre><code>powershell -ExecutionPolicy Bypass -File scripts/sync-to-opencode.ps1</code></pre>
      </div>
      <p class="src-ref">来源：<code>site/src/data/content.json</code>（install 区块）· <code>site/src/pages/docs/overview.astro</code></p>
    </section>

    <section id="first-task" class="doc-section">
      <h2>第一个任务</h2>
      <p>在项目目录执行 <code>opencode</code> 进入 TUI，然后使用 <code>/ultrawork</code> 提交任务：</p>
      <div class="code-block">
        <span class="code-block__lang">text</span>
        <pre><code>/ultrawork 写一份行业研究报告</code></pre>
      </div>
      <p><code>/ultrawork &lt;任务&gt;</code> 是一键编排命令：men 自动完成意图分诊、拆解、分发、验证、汇总，适合日常绝大多数任务。men 是唯一能 spawn 子 agent 的角色（si / ji / yi / xun / chi 均为 subagent，不能嵌套 spawn）。</p>
      <div class="wiki-note">
        <strong>协议计数说明</strong>：<code>ultrawork.md</code> 章节标题标注「9 步」，编号为 1–10。其中第 1–9 步为编排闭环（CERTAINTY → TRIAGE → PLAN → DISPATCH → COLLECT → EVALUATE → VERIFY → REPORT → LOOP），第 10 步 LEARN（自主学习触发，M7）在 REPORT 后追加，不阻塞主流程。
      </div>
      <p class="src-ref">来源：<code>.opencode/command/ultrawork.md</code> · <code>site/src/data/content.json</code>（commands）· <code>AGENTS.md</code></p>
    </section>

    <section id="verify" class="doc-section">
      <h2>验收产物</h2>
      <p><code>/verify &lt;角色名或路径&gt; [附加标准]</code> 用于独立验收某个角色的产物，由 chi 以 fresh-context Judge 身份执行双层验证：</p>
      <div class="code-block">
        <span class="code-block__lang">text</span>
        <pre><code>/verify ji -- 验证 ji 的代码产物</code></pre>
      </div>
      <ul>
        <li><strong>第 1 层 机械检查</strong> — 执行 <code>node scripts/verify.mjs &lt;目标&gt; --json --sid &lt;sid&gt;</code>，五项检查：<code>output-exists</code> / <code>secrets</code> / <code>todo-scan</code> / <code>structure</code> / <code>gate-exit-code</code>，任一 FAIL 即记机械失败。</li>
        <li><strong>第 2 层 语义复核</strong> — 机械检查全 PASS 后，chi 对内容匹配度、来源真实性、逻辑完整性、代码质量做独立判断，输出 Verdict：<code>PASS</code> / <code>FAIL</code> / <code>BLOCKED</code>。</li>
        <li><strong>报告落盘</strong> — Judge 报告写入 <code>.agents/state/sessions/&lt;sid&gt;/judge-&lt;角色&gt;.md</code>（best-effort）。</li>
        <li><strong>自主学习</strong> — 验证完成后触发 <code>node scripts/eval-metrics.mjs --sid &lt;sid&gt; --json</code>，计算 8 项 KPI（通过率、平均耗时、回归次数等，覆盖最近 10 次任务窗口）。</li>
      </ul>
      <SrcRef files={['.opencode/command/verify.md','scripts/verify.mjs','scripts/eval-metrics.mjs']} />
    </section>

    <section id="hyperplan" class="doc-section">
      <h2>规划复杂项目</h2>
      <p><code>/hyperplan &lt;项目&gt;</code> 用于复杂项目的访谈式规划，产出可执行项目计划但不执行：</p>
      <div class="code-block">
        <span class="code-block__lang">text</span>
        <pre><code>/hyperplan 设计一个多 Agent 协作系统</code></pre>
      </div>
      <ul>
        <li><strong>六项访谈</strong> — 逐项确认目标 / 范围 / 阶段 / 涉及角色 / 约束 / 验收，六项全部明确前不进入规划环节。</li>
        <li><strong>产出 plan envelope</strong> — 包含项目目标、范围与边界、阶段划分、任务依赖图、并行波次、涉及角色、验收标准总表、TODO List；验收标准必须机械可验证。</li>
        <li><strong>只规划不执行</strong> — 计划经用户确认后，再用 <code>/ultrawork</code> 按 plan 的 Wave 顺序分发执行。</li>
      </ul>
      <p class="src-ref">来源：<code>.opencode/command/hyperplan.md</code> · <code>site/src/data/content.json</code>（commands）</p>
    </section>

    <section id="next" class="doc-section">
      <h2>下一步</h2>
      <p>跑通第一个任务后，建议继续阅读：</p>
      <ul>
        <li><strong>协议规范</strong> — 10 步编排协议、5 项机械验证、事件类型与自主学习回路的完整说明（<a href={base + 'docs/protocols'}>docs/protocols</a>）。</li>
        <li><strong>架构设计</strong> — 系统拓扑、编排流程与验证体系全景（<a href={base + 'docs/architecture'}>docs/architecture</a>）。</li>
        <li><strong>自主学习回路</strong> — 每次 <code>/ultrawork</code>（第 10 步）与 <code>/verify</code>（第 5 步）完成后自动触发 <code>learn.mjs</code> / <code>eval-metrics.mjs</code>，经验写入 <code>knowledge/errors/</code> 与 <code>knowledge/patterns/</code>（best-effort，不阻塞主流程）。</li>
      </ul>
      <SrcRef files={['AGENTS.md','docs/guide/quickstart.md']} />
    </section>
  </WikiManual>
</BaseLayout>
